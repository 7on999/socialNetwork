
let initialState = {
};

type initialStateType = typeof initialState

const sideBar = (state:initialStateType=initialState, action:any):initialStateType => {
    return state;
}

export default sideBar;